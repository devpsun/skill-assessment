# 开发约定

## 开始工作

1. 阅读 `docs/progress.md`、`docs/requirements.md`、`docs/decisions.md` 及当前阶段对应的 `docs/roadmap.md`。
2. 核对分支、最新提交和工作区。保留用户及其他开发者已有改动。
3. 以用户最新明确指示为准。已确认的信息不重复追问；通常自行处理可逆的实现选择。
4. 当前仓库是 P0 设计基线。文档示例不是已存在的实现。引入命令后同步更新实际使用说明。

## 产品约束

- Python 承担评测核心；npm 启动、分发允许少量 JS/TS/sh。
- Windows x64 是首要验收平台。保持 Git Bash 友好，核心流程不依赖 PowerShell、WSL、Docker 或自动安装 nvm。
- 使用者可预装 Python 和 Agent。产品安装、初始化不从 GitHub、PyPI 或其他未配置的外部地址拉取工具依赖。
- 模型服务由 Agent 管理，默认复用已有配置。不要求用户提供模型服务地址、密钥或完整配置才能继续开发。
- 宿主、执行器、评分器可以使用同款或不同款 Agent。每个测试任务及评分任务应独立运行并保留归属信息。
- 评测只写评测工作区与报告；初次生成 eval 属于配套 Skill 的建例流程。目标 Skill 的演进只在用户明确要求时发生。
- 自定义执行器实现版本化协议，不针对未知 Agent 猜测接口，也不把安装配套 Skill 等同于执行器自动适配完成。

## 实现与验证

- 先定义版本化的配置、结果与适配契约，再扩展功能。上游不支持字段应明确报错，不能静默忽略后声称兼容。
- 路径使用跨平台 API；用户文本不拼接进 shell 命令。集中处理 `.cmd` 启动、编码、取消和子进程清理。
- 静态规格错误、质量建议、功能失败、执行错误、评分错误分别记录；未执行的检查不能显示为通过。
- 评分器看得到标准与证据；被测 Agent 不应收到隐藏断言、参考答案和评分器上下文。
- 记录已有 Agent 配置的非敏感可观测信息；不要将认证配置、完整环境变量或真实内部数据提交到仓库。
- 采用针对实际风险的测试。文档变更检查链接与一致性，不引入只重复文档内容的测试套件。
- 测试结果注明平台与类型：模拟执行器、真实 Agent、安装验证分别描述。Linux 上通过不能标为 Windows 已通过。
- 第三方代码或资源复用记录来源、固定版本和许可文件；业务样例使用自造、可重现数据。

## 持续协作

- 使用短期分支和小步 PR；一次变更围绕一个可验收目标。
- PR 写清问题、改变的行为、验证证据和实际限制。不得把未运行的测试写成已通过。
- 更新 `docs/progress.md`，记录完成内容、下一步及仍需真实环境验证的事项。
- 架构选择发生变化时更新 `docs/decisions.md`；产品范围变化时同步需求与路线图。
- 不仅依赖聊天历史保存开发状态。设计、实现和验证证据应能从仓库及 PR 中恢复。
