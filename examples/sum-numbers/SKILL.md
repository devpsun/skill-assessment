---
name: sum-numbers
description: Sum integer inputs using the bundled Python script when the user requests an exact integer sum.
---

Use [the summation script](scripts/sum_numbers.py) with the input integers as
separate arguments. Return its integer result.
