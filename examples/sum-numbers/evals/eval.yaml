schema_version: "1"
skill:
  path: ".."
engine:
  type: local
  command: ["{python}", "{config_dir}/../../fixture_engine.py", "{input_file}", "{output_file}"]
  isolation: controlled
cases:
  files: [cases/sum.yaml]
